name = "temp-mail-worker"
main = "src/index.ts"
compatibility_date = "2024-12-01"
workers_dev = true
account_id = "0187af8ffa0e426c1758c46d0b319a54"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "temp-mail-db"
database_id = "8c844e2a-40f4-4105-b29a-fb0c0169462c"

[triggers]
crons = ["0 3 * * *"]

[vars]
# Comma-separated list of allowed frontend origins (for CORS)
ALLOWED_ORIGINS = "http://localhost:3000"
# ADMIN_PASSWORD is stored as a Wrangler secret (not in this file).
# Set it with: cd worker && npx wrangler secret put ADMIN_PASSWORD
ADMIN_PASSWORD = "kt19911991"